/**
 * Ctrl + space = auto fill
 * Crtl + D = delete all
 * Alt + Shift + R = refactor (change the variable name for the whole code)
 * Ctrl + / = for comment all lines
 * Ctrl + Shift + / = single line comment 
 * Alt + up/down = move line up or down
 * Alt + Shift + m = make a method
 * Alt + Shift + l = make a variable 
 * @author admin
 *
 */


public class Test {
	public void name() {
		int car = 1;
		display(car);
	}

	private void display(int car) {
		System.out.println(car);
		String x = "Hello";
		System.out.println(x);
	}
}
